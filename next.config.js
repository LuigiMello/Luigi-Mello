/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'export',
  basePath: '/Luigi-Mello',
  assetPrefix: '/Luigi-Mello',
  images: {
    unoptimized: true,
  },
};
module.exports = nextConfig;
